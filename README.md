# Alex Task Board

Static task history board for Alex.

## How it works
- `index.html` renders cards from `log.json`.
- Update `log.json` to add new task entries.

## Deploy
- GitHub Pages from `main` / root.
